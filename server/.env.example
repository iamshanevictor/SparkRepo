# Environment configuration for SparkRepo backend

# Flask environment: development or production
FLASK_ENV=development

# JWT Secret Key for signing tokens. Keep this secret!
# You can generate a new key with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=

# JWT access token lifetime in seconds (default 86400 = 24h)
JWT_ACCESS_TOKEN_EXPIRES=86400

# Database connection string. If empty, falls back to local SQLite file sparkrepo.db
# Examples:
#   sqlite:///absolute/path/to/sparkrepo.db
#   postgresql+psycopg2://user:pass@localhost:5432/sparkrepo
DATABASE_URL=

# Allowed CORS origins (comma-separated or single origin)
# For local dev with Vite:
CORS_ORIGINS=http://localhost:5173
